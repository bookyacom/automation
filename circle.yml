machine:
  python:
    version: 3.5.1
  services:
    - docker

dependencies:
  post:
    - pip install --user -r requirements.txt

test:
  override:
    - echo "No tests found"


deployment:
  development:
    branch:
      - development
    commands:
      - echo "No deployment to development environment yet."
  production:
    branch:
      - master
    commands:
      - echo "No deployment to production environment yet."
